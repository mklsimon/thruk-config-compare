{
    "hosts": {
        "columns": ["name", "state"]
    },
    "services": {
        "columns": ["host_name", "state", "description", "host_address"]
    },
    "timeperiods": {
        "columns": ["name", "alias"]
    },
    "contacts": {
        "columns": ["name", "alias", "email"]
    }
}